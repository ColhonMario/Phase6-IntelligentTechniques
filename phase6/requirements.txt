# Minimum-version pins for Streamlit Community Cloud.
# Using >= instead of == so pip can pick wheels available for whatever
# Python version Cloud actually runs (sometimes ignores runtime.txt).

streamlit>=1.39.0
pandas>=2.2.0
numpy>=1.26.0,<3.0
scikit-learn>=1.5.0
lightgbm>=4.3.0
joblib>=1.4.0
matplotlib>=3.9.0
